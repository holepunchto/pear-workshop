# Exercise 1 - `pear init`

Create a new Pear Project. Open a terminal and run the following:

```sh
mkdir chat
cd chat
pear init ui -y
```

This will generate a Pear Desktop Application.

To finish up make sure dependencies are installed:

```sh
npm install
```

Run `pear help init` for more information about `pear init`.